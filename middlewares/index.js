const errorHandlerMiddleware = require('./errorHandlerMiddleware');
const {} = require('./productMiddlewares');
const {} = require('./salesMiddlewares');

module.exports = {
  errorHandlerMiddleware,
};
