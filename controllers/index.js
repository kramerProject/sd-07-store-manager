const { addProducts } = require('./addProducts');
const { deleteProducts } = require('./deleteProducts');
const { editProducts } = require('./editProducts');
const { findAllProducts } = require('./findAllProducts');
const { findIdProducts } = require('./findIdProducts');
const { findNameProducts } = require('./findNameProducts');
const { addSale } = require('./addSale');
const { editSale } = require('./editSale');
const { deleteSale } = require('./deleteSale');
const { findAllSale } = require('./findAllSale');
const { findIdSale } = require('./findIdSale');
const { findNameSale } = require('./findNameSale');

module.exports = {
  addProducts,
  deleteProducts,
  editProducts,
  findAllProducts,
  findIdProducts,
  findNameProducts,
  addSale,
  editSale,
  deleteSale,
  findAllSale,
  findIdSale,
  findNameSale,

};
